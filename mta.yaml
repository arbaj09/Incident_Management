_schema-version: 3.3.0
ID: incidenet-management
description: A simple CAP project.
version: 1.0.0
modules:
- name: incidenet-management-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: incidenet-management-auth
  - name: incidenet-management-db
  - name: incidenet-management-destination
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: incidenet-management-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: incidenet-management-db
  parameters:
    buildpack: nodejs_buildpack
- name: incidenet-management-app-deployer
  type: com.sap.application.content
  path: gen
  requires:
  - name: incidenet-management-auth
  - name: srv-api
  - name: incidenet-management-html5-repo-host
    parameters:
      config:
        HTML5Runtime_enabled: true
      content-target: true
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            HTML5.DynamicDestination: true
            HTML5.ForwardAuthToken: true
            Name: srv-api
            ProxyType: Internet
            Type: HTTP
            URL: ~{srv-api/srv-url}
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
  build-parameters:
    build-result: app/
    requires:
    - artifacts:
      - incident.zip
      name: incidenetmanagementincident
      target-path: app/
    - artifacts:
      - project1.zip
      name: project1
      target-path: app/
- name: incidenetmanagementincident
  type: html5
  path: app/incident
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: incidenet-management-destinations
  type: com.sap.application.content
  requires:
  - name: incidenet-management-auth
    parameters:
      service-key:
        name: incidenet-management-auth-key
  - name: incidenet-management-html5-repo-host
    parameters:
      service-key:
        name: incidenet-management-html5-repo-host-key
  - name: srv-api
  - name: incidenet-management-destination
    parameters:
      content-target: true
  parameters:
    content:
      instance:
        destinations:
        - Name: incidenet-management-html5-repository
          ServiceInstanceName: incidenet-management-html5-repo-host
          ServiceKeyName: incidenet-management-html5-repo-host-key
          sap.cloud.service: incidenetmanagement.service
        - Authentication: OAuth2UserTokenExchange
          Name: incidenet-management-auth
          ServiceInstanceName: incidenet-management-auth
          ServiceKeyName: incidenet-management-auth-key
          sap.cloud.service: incidenetmanagement.service
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: project1
  type: html5
  path: app/project1
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: incidenet-management-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      role-collections:
      - description: generated
        name: support (incidenet-management ${org}-${space})
        role-template-references:
        - $XSAPPNAME.support
      - description: generated
        name: admin (incidenet-management ${org}-${space})
        role-template-references:
        - $XSAPPNAME.admin
      tenant-mode: dedicated
      xsappname: incidenet-management-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: incidenet-management-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: incidenet-management-destination
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            HTML5.DynamicDestination: true
            HTML5.ForwardAuthToken: true
            Name: srv-api
            ProxyType: Internet
            Type: HTTP
            URL: ~{srv-api/srv-url}
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
    service: destination
    service-plan: lite
  requires:
  - name: srv-api
    group: destinations
    properties:
      forwardAuthToken: true
      name: srv-api
      url: ~{srv-url}
- name: incidenet-management-html5-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
- name: incidenet-management-html5-runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
